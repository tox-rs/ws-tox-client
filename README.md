# Tox WebSocket Client

A primitive client for ws-tox.

## Usage

Use `npm run server` to run the client. It will become available at <http://localhost:8080/>.

To get a list of available commands, use `/help`.
